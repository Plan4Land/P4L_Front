import styled from "styled-components";

export const ProfileImgContainer = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  width: 100%;
  height: 100%;
  overflow: hidden;
`;

export const EditImg = (file) => {};

export const ImgDownloader = ({ imgPath, width, height }) => {};
